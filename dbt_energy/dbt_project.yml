name: dbt_energy
version: 0.1.0
config-version: 2
profile: dbt_energy

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

seeds:
  dbt_energy:
    seed_carbon_intensity:
      +column_types:
        data_fetched_at: timestamp

models:
  dbt_energy:
    staging:
      +materialized: view
    marts:
      +materialized: table
